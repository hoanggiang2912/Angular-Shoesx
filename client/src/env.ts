export const PORT = 3000;
export const ROOT_URL = `http://localhost:${PORT}`;
export const API_URL = `${ROOT_URL}/api/v1`;
export const PRODUCTS_URL = `${API_URL}/products`;
export const CATEGORIES_URL = `${API_URL}/categories`;
export const USERS_URL = `${API_URL}/users`;
export const BILL_URL = `${API_URL}/bills`;
export const AUTH_URL = `${API_URL}/auth`;
